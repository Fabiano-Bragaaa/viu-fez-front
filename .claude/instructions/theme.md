# Theme

Always use theme values instead of hardcoded color strings.

Import colors from `@theme` and use the `colors` object for any color values in component props or logic.

For Tailwind/NativeWind class names, use theme tokens (e.g. `text-primary`, `bg-foreground`) instead of arbitrary values.
